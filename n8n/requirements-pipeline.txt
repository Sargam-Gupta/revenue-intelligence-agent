# Runtime deps for the n8n container pipeline (data -> variance -> narrative -> cache).
# Deliberately a subset of the project's root requirements.txt: NO streamlit/plotly,
# because the dashboard runs on Streamlit Cloud, not here. Keeping this lean avoids
# pulling pyarrow/streamlit (and their musl-wheel risk) into the n8n image.
anthropic>=0.40
pandas>=2.2
numpy>=2.0
pydantic>=2.9
python-dotenv>=1.0
