# Copy this file to `.env` and add your key. Never commit the real .env.
# Only needed to (re)generate narratives locally via scripts/pregenerate.py.
# The deployed dashboard reads the committed narrative cache and needs no key.
ANTHROPIC_API_KEY=your_api_key_here
